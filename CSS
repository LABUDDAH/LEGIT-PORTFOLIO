/* styles.css */
:root{ --accent:#0b5cff; --bg:#0f1724; --card:#0b1220; --muted:#9aa4b2; --glass: rgba(255,255,255,0.03); }
*{box-sizing:border-box}
body{margin:0;font-family:Inter, ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial; background:linear-gradient(180deg,#0a2f1d 0%, #0b2e3d 100%); color:#e6eef6; line-height:1.45}
.container{max-width:1000px;margin:36px auto;padding:24px}
header{display:flex;gap:16px;align-items:center}
h1{margin:0;font-size:28px}
p.lead{color:var(--muted);margin-top:6px}
.header-actions{display:flex;gap:10px;align-items:center;margin-top:6px}

.grid{display:grid;grid-template-columns:1fr 320px;gap:20px;margin-top:20px}
.card{background:var(--card);padding:18px;border-radius:12px;box-shadow:0 6px 18px rgba(2,6,23,0.6);}
a.btn{display:inline-block;padding:10px 12px;border-radius:8px;background:linear-gradient(90deg,var(--accent),#3b82f6);color:white;text-decoration:none;font-weight:600}

ul.skills, .tools{list-style:none;padding:0;margin:0;}
ul.skills{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}
li.skill{background:var(--glass);padding:8px;border-radius:8px;color:var(--muted);font-size:14px}

h2.section{margin-top:0;margin-bottom:12px}
.certs, .projects{display:flex;flex-direction:column;gap:10px}
.cert{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent); padding:10px;border-radius:8px}

.contact-list{display:flex;flex-direction:column;gap:6px}
.contact-list a{color:var(--muted);text-decoration:none}

footer{margin-top:28px;color:var(--muted);font-size:13px;text-align:center}

@media(max-width:880px){.grid{grid-template-columns:1fr}}
